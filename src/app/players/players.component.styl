@import "../common/styles/variables.styl"

md-card
    display: flex
    min-height: $card-height

    .player-info
        padding: 0 12px

    .bio
        font-size: $extra-small-font

    .invite-button
        margin-left auto

        button
            padding 5px 10px
            min-width 50px
